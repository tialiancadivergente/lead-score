apiVersion: apps/v1
kind: Deployment
metadata:
  name: capture-export-worker
  namespace: lead-score
  labels:
    app: lead-score
    component: capture-export-worker
    managed-by: kustomize
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: lead-score
      component: capture-export-worker
      managed-by: kustomize
  template:
    metadata:
      labels:
        app: lead-score
        component: capture-export-worker
        managed-by: kustomize
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001

      containers:
      - name: capture-export-worker
        image: leadscoreacr.azurecr.io/lead-score:v1.4.19
        imagePullPolicy: Always

        envFrom:
        - configMapRef:
            name: lead-score-config
        - secretRef:
            name: lead-score-secrets
        env:
        - name: SERVICE_BUS_CONSUMER_ENABLED
          value: "true"
        - name: SERVICE_BUS_CAPTURE_EXPORT_CONSUMER_ENABLED
          value: "true"
        - name: SERVICE_BUS_CAPTURE_EXPORT_MAX_CONCURRENCY
          value: "1"
        - name: SERVICE_BUS_LEAD_REGISTRATION_CONSUMER_ENABLED
          value: "false"
        - name: SERVICE_BUS_LEAD_SCORE_CONSUMER_ENABLED
          value: "false"
        - name: SERVICE_BUS_MARKETING_EXTRACT_CONSUMER_ENABLED
          value: "false"

        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "1536Mi"
            cpu: "1000m"

        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3

        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3

        startupProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 0
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 30

        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          runAsNonRoot: true
          runAsUser: 1001
          capabilities:
            drop:
            - ALL

      restartPolicy: Always
      dnsPolicy: ClusterFirst
